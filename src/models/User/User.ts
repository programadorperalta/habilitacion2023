export default interface Usuario{
    firstName: string,
    lastName: string,
    email:string,
    password: string,
    confirmPassword: string,
    //Cabe destacar que el registro o captura de la fecha y hora se hara en el back es por ello que no se encuentra
    //Plasmado aqui.
}
